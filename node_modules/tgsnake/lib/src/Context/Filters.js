"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.filter = void 0;
function filter(key, ctx) {
    const aliases = {
        cb: 'callbackQuery',
        msg: 'message',
        editMsg: 'editedMessage',
        editPost: 'editedChannelPost',
    };
    if (Array.isArray(key)) {
        for (const k of key) {
            if (k === 'any' || ctx[k] || ('className' in ctx && k === ctx.className))
                return true;
            let sk = k.split('.');
            if (sk.length) {
                if (ctx[aliases[sk[0]]] && ctx[aliases[sk[0]]][sk[1]] !== undefined) {
                    return true;
                }
            }
        }
    }
    else {
        if (key === 'any' || ctx[key] || ('className' in ctx && key === ctx.className))
            return true;
        let sk = key.split('.');
        if (sk.length) {
            if (ctx[aliases[sk[0]]] && ctx[aliases[sk[0]]][sk[1]] !== undefined) {
                return true;
            }
        }
    }
    return false;
}
exports.filter = filter;
