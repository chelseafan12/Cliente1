import { Storages } from '../platform.node.js';
export declare class BrowserSession extends Storages.BaseSession {
    private _name;
    constructor(name: string);
    load(): Promise<void>;
    save(): Promise<void>;
    private _loadCache;
    private _loadE2E;
    private _makeCache;
    private _makeE2E;
    toJSON(): {
        [key: string]: any;
    };
    toString(): string;
}
