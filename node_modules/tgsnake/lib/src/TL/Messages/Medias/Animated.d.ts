import { TLObject } from '../../TL.js';
import { Raw } from '../../../platform.node.js';
import type { Snake } from '../../../Client/index.js';
import { type sendAnimationParams } from '../../../Methods/Messages/SendAnimation.js';
import { PhotoSize } from './PhotoSize.js';
export declare class Animation extends TLObject {
    fileId: string;
    fileUniqueId: string;
    width: number;
    height: number;
    duration: number;
    thumb: Array<PhotoSize>;
    date: Date;
    dcId: number;
    filename?: string;
    mimeType?: string;
    fileSize?: bigint;
    constructor({ fileId, fileUniqueId, width, height, duration, thumb, date, dcId, filename, mimeType, fileSize, }: {
        fileId: string;
        fileUniqueId: string;
        width: number;
        height: number;
        duration: number;
        thumb: Array<PhotoSize>;
        date: Date;
        dcId: number;
        filename?: string;
        mimeType?: string;
        fileSize?: bigint;
    }, client: Snake);
    static parse(client: Snake, document: Raw.Document): Animation;
    resend(chatId: bigint | string, more: sendAnimationParams): Promise<import("../Message.js").Message>;
}
