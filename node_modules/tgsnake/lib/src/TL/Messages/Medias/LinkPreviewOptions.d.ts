import { Raw } from '../../../platform.node.js';
import { TLObject } from '../../TL.js';
import type { Snake } from '../../../Client/index.js';
export declare class LinkPreviewOptions extends TLObject {
    url?: string;
    preferSmallMedia?: boolean;
    preferLargeMedia?: boolean;
    showAboveText?: boolean;
    constructor({ url, preferSmallMedia, preferLargeMedia, showAboveText, }: {
        url?: string;
        preferSmallMedia?: boolean;
        preferLargeMedia?: boolean;
        showAboveText?: boolean;
    }, client: Snake);
    static parse(client: Snake, webpage: Raw.MessageMediaWebPage): LinkPreviewOptions;
}
