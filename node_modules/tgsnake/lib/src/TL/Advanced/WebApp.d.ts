import { TLObject } from '../TL.js';
import { Raw } from '../../platform.node.js';
import type { Snake } from '../../Client/index.js';
export declare class WebAppData extends TLObject {
    data: string;
    buttonText: string;
    constructor({ data, buttonText, }: {
        data: string;
        buttonText: string;
    }, client: Snake);
    static parse(client: Snake, webApp: Raw.MessageActionWebViewDataSentMe): WebAppData;
}
